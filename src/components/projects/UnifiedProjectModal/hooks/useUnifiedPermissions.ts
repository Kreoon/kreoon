/**
 * useUnifiedPermissions
 *
 * This hook is now embedded directly in useUnifiedProject.
 * Re-exported here for direct usage if needed.
 */
export { adaptContentPermissions, buildMarketplacePermissions } from '@/lib/unifiedPermissions';
