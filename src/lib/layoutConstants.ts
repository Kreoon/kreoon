// Altura real del contenido de la bottom nav movil (py-2 + icono 20px + label 10px + gaps).
// Fuente unica: MainLayout la usa para el padding del <main> y para setear la CSS var que
// lee KiroFloatingButton, para no repetir el numero en 2 lugares desincronizados.
export const MOBILE_BOTTOM_NAV_HEIGHT_PX = 64;

export const MOBILE_BOTTOM_NAV_CSS_VAR = '--kreoon-bottom-nav-h';
