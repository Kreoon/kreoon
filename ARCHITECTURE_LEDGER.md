# ARCHITECTURE_LEDGER.md — KREOON (ground truth)

> Verdad establecida del codebase. Claude Code LEE ESTO antes de explorar.
> Si el dato está aquí, NO grep/read para confirmarlo. Actualizar al descubrir algo nuevo (sección UPDATES abajo).
> Supabase project: `wjkbqcrxwsmvtxmqgiqc`. Rama activa: `feature/academy-v2-alexander`. StrictMode ON (main.tsx).

## FEED / PORTFOLIO
- `portfolio_items` = FUENTE DE VERDAD del feed. 1228 filas (697 `organization_content` + 531 `manual`).
- `portfolio_posts` = LEGACY. 158 filas, 21 migradas (resto = usuarios sin creator_profile, no forzar). No borrar.
- Trigger `sync_approved_content_to_portfolio`: crea fila privada por creator+editor cuando content → approved/paid. Solo 11/697 públicas. `is_public` filtra bien.
- Publicar a portafolio = UPSERT en portfolio_items (source_type='content_delivery'), NO cambio de status. content usa flags bool: is_published, shared_on_kreoon, is_portfolio_public, show_on_creator_profile.
- `feed_reactions`: CHECK constraint (love/fire/clap/wow/sad), NO enum PG. Contador desnormalizado reactions_count vía trigger. ReactionButton antes muerto, ya wireado.
- `saved_items`: genérico (item_type/item_id), SIN CHECK constraint. 'portfolio_item' válido sin tocar SQL.
- `followers`: 12 filas, casi todas rotas (apuntan a usuarios borrados, sin cascada). Tab "Siguiendo" nace vacía → empty state con SuggestedProfiles.
- RPC lectura feed: `get_feed_posts(tab, niche, cursor, limit)` SECURITY DEFINER, viewer via auth.uid() (NO parámetro cliente — hubo IDOR).
- feed-recommendations (edge fn): heurística JS pura (following+recency+engagement+diversidad+shuffle). Mal etiquetada como gemini. Reutilizable tal cual.

## DEAD CODE / NO TOCAR
- MUERTO: `adn-orchestrator`, `adn-continue`, `adn-orchestrator-lite`. Pipeline real = `generate-full-research`.
- NO TOCAR: `modules/social/*` = Social Hub (scheduling externo IG/TikTok, estilo Metricool). NO es el feed interno.
- Pasarelas muertas: Wompi, MercadoPago, Hotmart. Stripe = único cobro. Payouts talento = manual (DolarApp/Mercury).

## REPUTACIÓN / GAMIFICACIÓN
- `award_reputation_event` EXIGE `is_org_member()` → estudiantes/freelancers sin org NO emiten reputation_events. Racha necesita 2 entradas: RPC (org) + trigger en feed_reactions (cualquiera).
- Academia gamificación (streak_days/WeeklyMission) scoped a space_id. SEPARADO del UP. No confundir.
- `user_streaks` = global nuevo. "Día" = America/Bogota vía función `kreoon_today()`. Escritura solo server-side (trigger SECURITY DEFINER).
- Misiones: `mission_templates` + `user_daily_missions`. Asignación LAZY vía `get_daily_missions()` (no cron aunque pg_cron exista). Progreso server-side vía trigger en reputation_events. Cap anti-farmeo reacciones: 5/día.
- up-ai-copilot generador misiones (#60): NO llamar por usuario/día (costo). Solo refresh admin del pool.

## NOTIFICACIONES
- 3 tablas paralelas: `notifications`, `user_notifications`, `social_notifications`. USAR social_notifications (tiene UI: useSocialNotifications + dropdown).
- Web Push: `push_subscriptions` + `push_dedup_log` (throttle 5min). Trigger social_notifications → pg_net → edge fn `push-send` (VAPID, npm:web-push). Auth via header X-Internal-Secret (constant-time), notification_type whitelisteado, NO texto libre. Secreto en Vault.
- iOS: push SOLO con PWA instalada (≥16.4). Android/Chrome: completo.

## BUGS CONOCIDOS / PATRONES
- 🔴 REALTIME CHANNEL + StrictMode: topic literal crashea ("cannot add postgres_changes callbacks after subscribe"). FIX = topic único por mount: `nombre-{user.id}-{uuid}`. EXISTE EN ~30 HOOKS (useAchievements, useGlobalRanking, useMarketplaceNotifications, etc.) — bomba latente.
- VideoPlayerProvider: SocialFeed/useGlobalMute lo requiere. Envolver cualquier vista que renderice SocialFeed.
- ~~`portfolio_items_select_policy` = qual:true~~ RESUELTO Y VERIFICADO 2026-07-09: esa policy ya no existe, fue reemplazada (Fase 1 Cierre) por `portfolio_items_select_anon` (TO anon, solo public) + `portfolio_items_select_authenticated` (TO authenticated, 4 ramas: public/owner/org/followers + admin). Verificado en vivo con 2 usuarios reales (set role authenticated + request.jwt.claims): usuario ajeno lee 0 filas de un item privado, el owner lee 1. El dato de "pendiente" en el ledger estaba desactualizado.
- tsc baseline: ~1565-1568 errores PREEXISTENTES. No son nuestros. Solo validar cero-nuevos en archivos tocados (grep contra paths propios).
- 188/291 organization_members.role en legacy 'creator'.

## CONVENCIONES (no re-preguntar)
- RLS: `TO authenticated` (nunca public con subquery auth), `WITH CHECK`, whitelist columnas en UPDATE, GRANTs + `NOTIFY pgrst`.
- CORS en toda edge function. Secretos en Vault (patrón academy_fanout_secret).
- Aditivo. Schema-first. Commits en español. getPermissionGroup() (no roles hardcoded). finance/constants.ts.
- pg_cron ON, pg_net disponible.

## DESIGN SYSTEM
- SOLO tokens `nova-*` / `kreoon-*`. CERO hex hardcodeado, CERO gray-*.
- Componentes: KreoonGlassCard, KreoonButton, NovaCard, NovaButton, KreoonSkeleton (loadings). PageHeader (compact en móvil, breakpoint md=768).
- Tipografía BricolageGrotesque. Colores: #8B5CF6 / #00D9FF / #FF00E5 / #00FF88.
- Móvil: useIsMobile() = 768px. safe-area env(). targets 44px. Kanban default vista lista <768.

## PENDIENTES ABIERTOS (carryover)
- [x] Endurecer portfolio_items_select_policy — RESUELTO Y VERIFICADO 2026-07-09 (ver BUGS CONOCIDOS).
- [x] `useCreatorProfile.ts` select('*') sobre creator_profiles — RESUELTO 2026-07-09, whitelist aplicada (mismo patron que useCreatorPublicProfile).
- [x] npm run build tras workbox.importScripts — VERIFICADO 2026-07-09 (2 corridas independientes): pasa, `dist/sw.js` genera correctamente `importScripts("push-sw.js")`. Sin warnings de PWA/workbox.
- [x] ~37 hooks/componentes con topic realtime literal — RESUELTO 2026-07-09 (commit `4f3d3398`, subagente refactor-realtime). Verificado grep = 0 literales sin interpolar en src/hooks y src/components.
- [x] tsc verificado 2026-07-09 (corrida completa, ~35min, 3925 líneas): 59 errores en archivos tocados de la consolidación, los 59 confirmados 100% baseline preexistente vía diff exacto contra los commits de hoy (no solo heurística — se revisó línea por línea contra el diff real). Cero errores nuevos introducidos.
- [ ] Env var Vercel: VITE_VAPID_PUBLIC_KEY (manual Alexander).
- [ ] Conectar academy_lesson_completed / campaign_application a rachas (localizados, no wireados).
- [ ] Auditar 137 portfolio_posts no migrados (autores activos vs muertos).
- [ ] Mostrar racha en perfil público requiere columna pública nueva (current_streak no expuesto hoy) — no se hizo, spec lo marcaba opcional.
- [x] Bottom nav de Academia — RESUELTO (post Fase 3.5): 6 rutas de sesión envueltas en MainLayout con nav propio (Inicio/Cursos/Feed/Calendario + Más). Ver UPDATES para detalle.
- [ ] Academia mobile-first completo (páginas internas, leaderboard/map/retos/admin sin MainLayout) — parcial. Rediseño visual completo de cada página sigue sin hacerse, necesita captura de pantalla real para no adivinar decisiones de UI. Gating de rol ya verificado correcto para el modelo de datos actual (ver BUGS/UPDATES).
- [ ] Si Academia empieza a usar el rol `instructor` de verdad (hoy 0 filas en producción), `AcademiaManagePage.tsx` necesita agregar ese check — hoy solo mira `owner_id`.
- [ ] Pool de 3 players HLS reciclables (prev/current/next) — APROBADO por Alexander, diferido: implementar SOLO despues de validar Fase 3.7 en dispositivo real.
- [ ] Fix masivo de 136 thumbnails con host equivocado (UPDATE del host cdn.kreoon.com -> vz-78fcd769-050.b-cdn.net solo en patron /{uuid}/thumbnail.jpg) — causa confirmada, pendiente aprobacion.
- [ ] Fase 4 Recap pendiente.
- [ ] DROP del overload viejo de get_feed_posts (5 params, sin p_seed) — sin caller conocido, limpiar en proxima sesion de BD.

## UPDATES (append-only — Claude Code agrega descubrimientos aquí)
2026-07-10 | Fase 3.6 (bottom nav por rol, MainLayout.tsx): client y admin ganaron bottom nav movil (antes solo creator/editor/Academia). Client: Hub(/client-dashboard)·Feed·Talento(/marketplace exacto)·Campañas(/marketplace/my-campaigns)·Más. Admin: Hub(/dashboard)·Feed·CRM(/crm)·Usuarios(/talent)·Más — gateado por `isAdmin` (permission group), NO por `activeRole==='admin'` literal, asi digital_strategist/creative_strategist/community_manager (caen en el mismo branch "default" del switch de MainLayout por ser fallback, pero permission group 'talent') no ganan esta nav (sin cambio de comportamiento para ellos). `MoreMenuSheet` ahora acepta prop `items?: MoreMenuItem[]` para override — un solo componente data-driven en vez de 3 duplicados; creator/editor siguen con su default hardcoded original (dashboardHref), sin cambios. Items `platformRootOnly` de admin (Pagos Pendientes/Papelera/Módulos/QA) filtrados con `isPlatformRoot` de `useOrgOwner()`, mismo criterio que Sidebar.tsx. StreakWidget oculto solo para admin (racha = actividad de creación, no aplica); se mantiene visible para el resto de roles que caen en el branch default (son grupo talent). SocialNotificationsDropdown (campanita) agregada a headers de client y admin — antes faltaba en ambos. NO existe ruta de CRM para rol client — el bullet "CRM(org)" del spec de Fase 3.6 para el Más de client se omitió (CRM de plataforma es admin-only, /crm). "Usuarios" de admin mapea a `/talent` (UnifiedTalentPage, "Talento & Equipo") — no existe una página `/admin/users` dedicada, es la página real de gestión de miembros/talento de la org.
2026-07-10 | `src/components/layout/MobileBottomNav.tsx` es codigo muerto (verificado grep: unico match es el propio archivo) — nav generica con rutas que no existen en la app actual (/explore). No se toco (fuera de scope), queda documentado por si aparece en una limpieza futura.
2026-07-10 | Fase 3.7 feed TikTok-level: (1) resume-position en Map modulo-level de useHLSPlayer.ts (post_id->tiempo, TTL 2h, cap 200, NO localStorage a proposito — sesion de app solamente); un solo listener 'loadedmetadata' cubre los 3 codepaths (MP4/HLS nativo/hls.js). (2) Mute global persistido en localStorage 'kreoon_feed_muted' (VideoPlayerContext), default true solo primera visita; callback onForcedMute (useHLSPlayer->HLSVideoPlayer->SocialFeedCard->SocialFeed) re-sincroniza la persistencia cuando iOS bloquea autoplay con audio. (3) preloadHLSVideo ahora precarga buffer real (4-8s segun effectiveType) no solo manifest, y NO precarga nada en 2g/slow-2g/saveData; en iOS Safari (sin MSE) inyecta <link rel=preload as=fetch> del manifest (cap 4, FIFO). (4) SW/Workbox (vite.config.ts): .m3u8 NetworkFirst (timeout 3s, 50 entradas 1h) + .ts/.m4s CacheFirst (150 entradas, 36h, purgeOnQuotaError) — verificado en dist/sw.js que conviven con importScripts push-sw.js. (5) hls.js: capLevelToPlayerSize + capLevelOnFPSDrop + backBufferLength:10 en baseConfig. (6) startLevel HIBRIDO (decision cerrada por Alexander): movil viewport<768 + conexion no-wifi-confirmada -> startLevel:-1 (shouldUseLowStartLevel(), override unico en getOptimalHlsConfig); desktop/wifi conserva el piso min-720p historico. (7) KAE feed_video_start (ms_to_first_frame/from_cache/effective_type), sampleado 20%, cronometro arranca SOLO en transicion isActive false->true (swipe real), no en pausa/reanuda manual. (8) FeedPage precarga los primeros 2 videos apenas llegan datos (ref one-shot). Pool de 3 players reciclables: APROBADO pero diferido a proxima iteracion, solo tras validar 3.7 en dispositivo real.
2026-07-10 | Bunny NO tiene Token Auth en la libreria del feed (BUNNY_LIBRARY_ID) — getBunnyVideoUrls() nunca agrega query params, cache-key del SW = URL completa sin conflicto. Si se activa Token Auth ahi, revisar las 2 reglas de runtime caching de vite.config.ts.
2026-07-16 | MCP OAuth follow-up (mismo día, post-deploy): el DCR/PKCE inicial dejaba 2 huecos que un review de seguridad detectó — `redirect_uri` no se validaba contra lo registrado para el `client_id` (cualquiera armaba un link a `mcp.kreoon.com/oauth/authorize?redirect_uri=https://evil.com`, la víctima entraba su API key real en el dominio legítimo, el code terminaba en el sitio atacante — authorization code interception clásico) y PKCE era opcional (un cliente atacante podía omitir `code_challenge` y saltarse esa defensa). Fix: `validateAuthorizeRequest()` en `api/index.ts` ahora exige `client_id` registrado vía `/oauth/register`, `redirect_uri` con match EXACTO contra `mcp_oauth_clients.redirect_uris`, y PKCE (S256) obligatorio — se aplica tanto en GET como POST `/oauth/authorize`, y `/oauth/token` ya no trata `code_challenge` como opcional. Cualquier integración vieja sin DCR deja de funcionar (intencional).
2026-07-16 | Fase 1 MCP portafolio (kreoon-mcp-server/src/tools/portfolio.ts): 6 tools (get_my_portfolio, generate_portfolio, update_portfolio_block, publish_portfolio, add_portfolio_item, list_portfolio_items), scope profiles:write (client lo excluye solo por scope, GROUP_SCOPES.client no lo incluye). resolveTargetUserId fuerza talent a su propio user_id; admin puede apuntar a miembros de su org (valida organization_members). ensureCreatorProfile auto-siembra creator_profiles si el talento nunca pasó por el wizard. generate_portfolio llama portfolio-ai (acción nueva "generate_portfolio") que devuelve ProfileBlock[] con estilos ricos vía IA — requirió agregar un bypass de auth server-to-server en portfolio-ai (comparación EXACTA contra el secret de service role, nunca decode de JWT sin firma) porque optimize_creator_profile YA sufría el mismo problema en silencio (fallback algorítmico sin que nadie lo notara). Persistencia vía RPCs nuevos mcp_save_portfolio_blocks/mcp_publish_portfolio_blocks (SECURITY DEFINER, sin auth.uid()) — los RPCs viejos save_profile_blocks/publish_profile_blocks dependen de auth.uid()=NULL desde el MCP, nunca hubieran funcionado.
2026-07-16 | Fase 2 MCP webhooks salientes (mcp_webhook_dispatch edge function + kreoon-mcp-server/src/tools/webhooks.ts + webhookEmitter.ts): la tabla `mcp_webhooks` existía desde mayo con columna `secret_hash` (comentario "HMAC secret hasheado") pero JAMÁS se podía usar para firmar — un hash es one-way, no se puede recuperar para calcular una firma HMAC en cada entrega (a diferencia de mcp_api_keys.key_hash, que solo se COMPARA). Se renombró a `secret` (texto plano) y se cerró la sobre-exposición resultante: la policy SELECT vieja permitía al dueño/admin leer la fila completa (antes daba igual, era un hash; ahora expondría el secreto) — se reemplazó por `USING (false)` + vista `mcp_webhooks_safe` (sin la columna secret) para lectura desde frontend. Dispatcher real: HMAC-SHA256 (Web Crypto de Deno) sobre el payload, headers X-Kreoon-Event/X-Kreoon-Signature, auto-desactiva el webhook tras 5 fallos consecutivos (mismo criterio que Stripe). 3 tools nuevas admin-only (register_webhook/list_webhooks/revoke_webhook, ADMIN_ONLY_TOOLS en permissions.ts). Eventos cableados: portfolio.published (publish_portfolio) y script.generated (generate_script, ambos modos). emitWebhookEvent es fire-and-forget — nunca rompe la respuesta de la tool que lo dispara.
2026-07-16 | Fase 3 MCP import de diseño externo: nuevo BlockType `external_design` en el Profile Builder (src/components/profile-builder/types/profile-builder.ts) + componente ExternalDesignBlock.tsx, registrado en LOS 3 puntos que requiere (BlockRenderer.tsx del editor — obligatorio por TS al ser `Record` completo; blocks/index.ts del perfil público — NO obligatorio por tipos, `Partial<Record>`, fácil de olvidar y cae en fallback silencioso si se omite). A diferencia del video_embed existente (detectProvider por `includes()` de substring, sin whitelist de hostname, iframe SIN `sandbox` — ver VideoEmbedBlock.tsx), este bloque valida el hostname EXACTO (`new URL(url).hostname`, solo figma.com/www.figma.com) y el iframe de Figma lleva `sandbox="allow-scripts allow-same-origin allow-popups"`. Gamma/Stitch/Canva no tienen embed oficial — se soportan como `<img>` plano (imagen real, verificada por Content-Type vía HEAD si la extensión no es obvia, nunca HTML/script). Tool MCP `import_external_design` en portfolio.ts inserta el bloque directo en profile_builder_blocks (draft) sin pasar por los RPCs mcp_save_portfolio_blocks (esos reemplazan TODO el set; esto es un append incremental). CSP del proyecto (vercel.json) tiene `frame-src https:` — sin restricción de dominio a nivel CSP, la única barrera real es la validación de hostname en código.
2026-07-16 | Fixes post-deploy Fase 2/3 (security review detectó los 3): (1) XSS en ExternalDesignBlock.tsx — el iframe/img confiaba ciegamente en figma_embed_url/image_url guardados en BD; ahora se re-valida SIEMPRE en render (safeFigmaEmbedUrl/safeImageUrl, hostname+protocolo), y se sacó `allow-same-origin` del sandbox del iframe (combinado con allow-scripts permite que un iframe se auto-libere del sandbox). (2) SSRF en portfolio.ts (looksLikeImage) y webhooks.ts (register_webhook) — un fetch/HEAD server-side a una URL de args sin resolver DNS primero deja usar el MCP como proxy contra red interna/metadata de nube; nuevo módulo compartido `kreoon-mcp-server/src/ssrfGuard.ts` (isSafePublicHost: dns.lookup + rechazo de rangos privados/loopback/link-local/CGNAT) usado en ambos. (3) mismo SSRF en el dispatcher real (mcp-webhook-dispatch, Deno) — webhook.url la elige un admin al registrar, no es input de confianza a nivel infra; se re-valida en CADA entrega (no solo al registrar, por DNS rebinding) con Deno.resolveDns, más redirect:"manual" y rechazo de 3xx en ambos (dispatcher Deno y el HEAD de portfolio.ts) para no seguir un redirect hacia un destino no validado.
2026-07-16 | MCP server (`kreoon-mcp-server/`) pasó de scopes fijos elegidos al crear la key (target_type client/talent/organization) a rol-aware: `resolveAuthContext()` en `api/index.ts` re-lee `organization_members.role`+`is_owner` EN CADA REQUEST y deriva `PermissionGroup` (admin/talent/client) vía `src/permissions.ts` — si el rol baja o la membresía se borra, la key pierde permisos en la siguiente llamada, no cuando alguien la revoca a mano. Segunda capa `isToolAllowedForGroup()` (allow-list por tool) porque el scope `campaigns:write` agrupa privilegios muy distintos (crear item de contenido vs. aprobar guión vs. crear cliente/campaña) — client group NO puede crear/asignar/generar IA, solo leer+aprobar guión de su propia org. `mcp-key-manager` edge function ya no pregunta target_type: cualquier miembro activo puede autoservirse una key, el grupo sale de su rol real. Rate limit movido de Map en memoria (se perdía en cada cold start de Vercel) a RPC atómica `mcp_check_rate_limit` (tabla `mcp_rate_limit_counters`). OAuth: el "code" ya NO es la api_key cruda en la URL — es un valor opaco de un solo uso en tabla `mcp_oauth_codes` con soporte PKCE (S256); se agregó DCR real (`POST /oauth/register` + tabla `mcp_oauth_clients`) porque claude.ai pedía un Client ID a mano al no encontrar `registration_endpoint`. Fixes de IDOR: `manage_campaign_application` (campaigns.ts) validaba la aplicación solo por `id`, sin chequear que la campaña fuera de `auth.org_id` — cross-org write confirmado, ahora usa join `marketplace_campaigns!inner(organization_id)`. `create_content_item`/`update_content_item`/`assign_content_team` (operations.ts) y `create_marketplace_project`/`assign_editor_to_project` (projects.ts) ahora validan que client_id/product_id pertenezcan a la org y que creator_id/editor_id sean miembros reales (org) o perfiles públicos del marketplace (creator_id en proyectos), antes insertaban FKs de args sin verificar. AuthScope se redujo a los 6 scopes que TOOL_SCOPES realmente usa (se sacaron adn:*/wallet:*/scripts:read/profiles:read/creators:write/social:read/analytics:read, muertos desde que Wallet y ADN V3 salieron del MCP).
2026-07-10 | 🔴 Thumbnails rotos del feed — causa confirmada con curl: cdn.kreoon.com es pull zone del STORAGE de Bunny (rutas /marketplace/portfolio/... dan 200) pero NO de la stream library — las rutas formato stream /{videoId}/thumbnail.jpg dan 404 ahi (mismo path en vz-78fcd769-050.b-cdn.net da 200). 136 portfolio_items publicos afectados (patron exacto ^https://cdn.kreoon.com/{uuid36}/thumbnail.jpg$). Fix masivo pendiente (decidido no aplicar aun): UPDATE del host solo en ese patron. Mitigado en UI: HLSVideoPlayer ahora muestra fallback de marca (gradiente nova + logo) si el poster falta o da 404 — nunca pantalla negra.
2026-07-10 | getSafeAreaBottom (useKiroResponsive.ts) crasheaba con NotFoundError en removeChild — el probe DOM se ejecuta durante render (useState initializer, doble bajo StrictMode) y el nodo podia ya no ser hijo de body. Fix: try/catch + div.remove() (no lanza sin padre) + probe position:fixed invisible. Patron general: NUNCA document.body.removeChild(x) para cleanup de probes, siempre x.remove().
2026-07-10 | KreoonButton ahora soporta asChild via @radix-ui/react-slot (patron shadcn). Con asChild NO se inyecta spinner de loading ni type/disabled (Slot exige un solo hijo; atributos de <button> no existen en <a>). Consumidores que ya lo pasaban (y antes filtraba React al DOM como atributo invalido): ErrorBoundary.tsx y RouteErrorBoundary.tsx.
2026-07-10 | Fase 3.7b (mas optimizacion video + aleatorizador): (1) buffers del feed recortados en fastStart (15s adelante / 30MB, antes 30s/60MB) + abrEwmaFastVoD:2 — feed de swipe, no cine. (2) preloadThumbnail() (new Image, dedupe Set) para los siguientes 2 slides — poster instantaneo. (3) iOS nativo: prefetchFirstSegmentsNative() parsea master->variante->primeros 1-2 segmentos via fetch() que pasan por el SW -> quedan en bunny-hls-segments-v1 (CacheFirst) y el player nativo de Safari los encuentra en cache. Best-effort con dedupe. (4) Aleatorizador feed v2 (migracion 20260710160000): el jitter aditivo ±0.15h era irrelevante vs posts que difieren en DIAS — ahora jitter MULTIPLICATIVO ±25% sobre la edad + capa exploracion (h_explore<0.2 -> edad efectiva x0.4, subconjunto distinto por seed). Verificado en vivo: 2 seeds distintos comparten solo 5/20 posts y 0 en la misma posicion; mismo seed = 30/30 identico (paginacion estable); 0 autores consecutivos (round-robin intacto). OJO: existe un overload viejo de get_feed_posts SIN p_seed (5 params) — no se toco, el cliente siempre pasa p_seed; candidato a DROP en una limpieza futura.
2026-07-10 | 🔴🔴 INCIDENTE (CERRADO mismo dia): dev/sync/jarvis.kreoon.com apuntaban por DNS a 194.163.161.151 pero esa IP YA NO era el VPS de Alexander — servia un casino "Corredor777" (Apache/PHP, cert invalido, host key SSH cambiado). La cookie `auth` con password hash en .kreoon.com la seteaba ese tercero (subdomain takeover efectivo). CIERRE: Alexander elimino los registros DNS A de los 3 subdominios. Ademas se quito 'dev.kreoon.com' de la allowlist de `n8n-proxy` (posteaba payloads server-side a cualquier dominio de esa lista — funcion redeployada 2026-07-10). Jarvis v2, n8n y CouchDB del VPS viejo NO existen mas; si se re-montan, usar dominios/host nuevos y NUNCA re-agregar a la allowlist dominios sin control verificado. Queda a criterio de Alexander: rotar credenciales que viajaran a esos subdominios mientras el DNS estuvo tomado.
2026-07-10 | Post-deploy cierre: TODO lo de hoy esta en produccion (push ec457f69): bottom nav por rol (client/admin), Fase 3.7 completa (preload+resume+mute persistente+SW cache HLS), startLevel hibrido (3d29cf2e), fixes (safe-area crash, KreoonButton asChild, favicon.ico, poster fallback), optimizacion 3.7b (buffers cortos, preload thumbnails, prefetch segmentos iOS) y aleatorizador feed v2 (migracion aplicada). Pendientes vivos consolidados en la seccion PENDIENTES ABIERTOS.
2026-07-09 | Perfil público real: rutas `/p/:username` y `/@:username` -> `PublicCreatorPage.tsx` (NO existe `/talent/:username`). Campo real = `creator_profiles.slug` (433/433 poblado); `username` es columna muerta (0/433 filas, nunca escrita).
2026-07-09 | El perfil público NO es hardcoded — es un builder de bloques (`profile_blocks`, `builder_config`, 20+ BlockType: hero_banner/portfolio/reviews/stats/services/testimonials/etc.) renderizado por `ProfilePageRenderer` + `PublicBlockRenderer` (`src/components/profile-viewer/`). Antes de tocar el hero o agregar secciones, revisar `BLOCK_DEFINITIONS` en `src/components/profile-builder/types/profile-builder.ts`.
2026-07-09 | `PublicCreatorPage.tsx` YA tenía meta tags OG/Twitter completos + canonical + `ProfileShareButton` (react-helmet-async) — Paso 2 de SEO estaba resuelto de antes, no se re-construyó.
2026-07-09 | 🔴 CRÍTICO (ya en prod, no introducido por Fase 3): `creator_profiles` RLS SELECT es pública (`is_active=true OR own`) y `useCreatorPublicProfile.ts` hacía `select('*')` sobre esa tabla en 3 lugares -> exponía `stripe_account_id`/`whatsapp_phone`/`payout_method` a cualquier visitante sin sesión. Fix aplicado: whitelist explícita de columnas (`PUBLIC_CREATOR_PROFILE_COLUMNS`) igual a lo que `mapProfileRow()` realmente usa. RLS solo filtra FILAS, nunca columnas — cualquier `select('*')` sobre una tabla con policy pública es sospechoso por default.
2026-07-09 | `followers` -> trigger `on_follow_notification` (AFTER INSERT) ya emite `social_notifications` server-side. `FollowButton.tsx` (`src/components/social/FollowButton.tsx`) espera `profileId` = auth `user_id` (no `creator_profiles.id`) — reusable tal cual en cualquier vista nueva, cero backend nuevo necesario para follow.
2026-07-09 | KAE = Kreoon Analytics Events. Hook `useAnalytics()` (`src/hooks/useAnalytics.ts`), método `track({event_name, event_category, properties})`, funciona sin sesión (anonymous_id via localStorage), envía a edge fn `kae-track`. Ya usado para profile_view en `PublicCreatorPage.tsx`.
2026-07-09 | `hero_banner` block (builder) NO tiene nivel/racha — se agregó como overlay fijo fuera del sistema de bloques (`PublicCreatorPage.tsx`, badge `Nivel {level}` top-left) en vez de modificar el bloque genérico. `StreakWidget`/`useStreak()` están hardcoded a "mi propia racha" (auth.uid()) — NO sirven para mostrar la racha de OTRO usuario visto públicamente; requeriría exponer `current_streak` vía columna pública nueva. Se dejó pendiente (spec lo marcaba opcional: "racha si aplica").
2026-07-09 | Hire desde perfil público NO reimplementa el flujo — solo navega a `/marketplace/creator/:id` (ya existe `HiringWizard`/`stripe-creator-hire` ahí). Gating: visible si `!user` (CTA lleva a login/registro implícito en esa página) o si `getPermissionGroup(roles[0]) === 'client'`; oculto si es el propio perfil.
2026-07-09 | `useCreatorProfile.ts` select('*') — RESUELTO (consolidación pre-merge): whitelist `CREATOR_PROFILE_COLUMNS` aplicada al select por `user_id` (linea ~161). El insert().select('*') (linea ~233) se dejó intacto, es seguro (siempre devuelve la fila que el propio caller acaba de crear con su propio user_id).
2026-07-09 | Consolidación pre-merge (subagentes verificador-tsc + builder-prod + refactor-realtime): tsc corrida completa tarda ~35min en esta máquina (proceso llega a ~2.8GB RAM) — es normal, NO matar el proceso, esperar. `npm run build` en cambio es rápido y confirmó 2 veces que `workbox.importScripts: ['push-sw.js']` (vite.config.ts) genera correctamente `dist/sw.js` con `importScripts("push-sw.js")` embebido, sin warnings de PWA.
2026-07-09 | Bug baseline real detectado (NO se tocó, fuera de scope): `useGlobalRanking.ts` tiene 17 errores TS por columna `youtube` que no existe en `profiles` según los tipos generados (SelectQueryError) — sugiere un select con columna renombrada/eliminada del schema real. Revisar en una sesión futura si el ranking global funciona en runtime o está silenciosamente roto.
2026-07-09 | 🔴 Bug real de mute en feed inmersivo (Fase 3.5): había 3 fuentes de mute desincronizadas — `VideoPlayerContext.isGlobalMuted` (sin usar en el render real), `SocialFeed`'s `audioUnlocked` local (manejaba el ícono), y `currentMuted` interno de `useHLSPlayer` (manejaba el audio real, mutado imperativamente vía ref sin nunca re-sincronizar el ícono). Fix: `isGlobalMuted` (contexto) es ahora la única fuente; default cambiado a `true` (autoplay iOS exige muted=true al arrancar); el toggle llama `toggleGlobalMute()` (React state) y un único `useEffect` en `SocialFeedCard` empuja `playerRef.setMuted(isGlobalMuted)` — nunca más toggle imperativo directo sin pasar por state.
2026-07-09 | Feed inmersivo — acciones (like/save/share) y caption vivían en `bottom-28`/`bottom-8` fijos, se metían debajo de la bottom nav en creator/editor. Fix: `bottom-[calc(var(--kreoon-bottom-nav-h,0px)+env(safe-area-inset-bottom)+16px)]` (mismo patrón ya usado en FAB de FeedPage, Fase 2.5). KIRO comparte el corner default `bottom-right` con la columna de acciones — `KiroWidget.tsx` ahora la corre a `bottom-left` en `/feed` SOLO si el usuario nunca la movió manualmente (no pisa una elección explícita).
2026-07-09 | `get_feed_posts` tenía 2 bugs reales: (1) el filtro `p_niche` no dependía de `p_tab`, pero el bug real era que `FeedPage.tsx` no auto-seleccionaba ningún nicho al entrar a "Por nicho" (quedaba `null` → sin filtro → igual a "Para ti") — fix: auto-selecciona el primer nicho disponible. (2) CERO lógica de diversidad — posts venían agrupados por autor. Fix: `ROW_NUMBER() OVER (PARTITION BY author_user_id ORDER BY rank_score)` + `ORDER BY author_rank ASC` = round-robin real (verificado con SQL: 0 violaciones de "mismo autor consecutivo" en 100 filas con autor de 40 posts). Se agregó `p_seed` (jitter chico, ±0.15h en el score) para variar el orden entre refreshes sin romper el ranking real — verificado que dos seeds distintos producen órdenes distintos. `feed-recommendations` (edge fn con lógica de diversidad) NUNCA se llama desde este feed — es código muerto para este propósito, solo lo usa `useRecommendations.ts` (otra feature). No confundir ambos sistemas.
2026-07-09 | `SuggestedProfiles.tsx` sugería CUALQUIER profile (incluidos `client`/marcas) porque el candidate pool salía de `profiles` sin filtrar. Fix: solo se sugieren `user_id` que tengan fila en `creator_profiles` (`is_active=true`) — es el filtro correcto y simple ("¿es creador/talento real en la plataforma?"), más robusto que mapear roles legacy de `organization_members`. Se agregó priorización por nicho (`categories` overlap con el propio perfil del viewer).
2026-07-09 | Academia — modelo de roles real: `academy_memberships.role` (NO `academy_space_members`, ese nombre no existe). En producción HOY solo hay 2 filas, ambas `role='student'` — el rol `instructor` existe en el tipo TS (`useAcademyJoinSpace.ts`) pero CERO uso real en datos. El gating de `/academia/:slug/gestionar` (`AcademiaManagePage.tsx`) usa solo `academy_spaces.owner_id === user.id` (chequeo interno al componente, no a nivel de ruta — la ruta usa `ProtectedRoute allowNoRoles` genérico) y es CORRECTO para el modelo de datos actual (no hay instructors reales que excluir). Redisenio mobile-first completo de Academia (Fase 3.5 Bloque 4) NO se hizo — requiere la captura de pantalla real referenciada en el prompt para no adivinar decisiones de UI a ciegas; queda como pendiente con este hallazgo documentado.
2026-07-09 | Root cause de "Academia no tiene bottom nav como el resto de la app": NINGUNA ruta de `/academia/*` envolvía `<MainLayout>` en `App.tsx` — es un módulo standalone que nunca se integró al shell compartido (por eso "se ve como web": sin header, sin AccountMenu, sin nav). Fix (post Fase 3.5, a pedido directo): se envolvieron en `<MainLayout>` las 6 rutas de sesión de estudiante aprobadas — `/academia/:spaceSlug` (home), `/classroom`, `/feed`, `/post/:postId`, `/dm`, `/calendar`, `/members` — quedaron AFUERA a propósito: `/academia` y `/academia/explorar` (públicas/marketing), `/gestionar` y `/admin` (management), `/:courseSlug/edit` (editor de curso) y `/:courseSlug/learn` (reproductor de lección, full-screen inmersivo como el resto de la app con video). `MainLayout.tsx` detecta `isAcademiaSpaceSession` vía `useParams().spaceSlug` + exclusión de sufijos (`/gestionar`,`/admin`,`/edit`,`/learn`) y renderiza un branch NUEVO con prioridad sobre los 4 branches por rol (cualquier rol navegando Academia ve este nav, no el suyo de la app principal). Nav propio: Inicio/Cursos/Feed/Calendario + `AcademiaMoreMenuSheet` (Miembros/Ranking/Retos/Mapa) — NO se reusó `MoreMenuSheet` genérico porque sus items (Academia/Kreoon IA/Mis Cobros/Config) no encajan estando ya adentro de Academia. Pendiente: leaderboard/map/retos/admin siguen sin MainLayout — clickear esos links desde el "Más" saca al usuario del chrome nuevo (inconsistencia conocida, aceptada para este scope).
2026-08-11 | RATE LIMITING estaba 100% inoperante en TODA la plataforma, por DOS bugs independientes. (1) `_shared/rate-limiter.ts` llama `rpc('check_rate_limit', {p_key, p_limit, p_window_start})` pero la firma real es `check_rate_limit(_identifier, _identifier_type, _action_type, _max_attempts, _window_minutes, _block_minutes)` — PostgREST no resuelve, el wrapper cae en su rama fail-open y devuelve `allowed:true` SIEMPRE. (2) Aun llamando con la firma correcta, la RPC lanzaba `42P10: no unique or exclusion constraint matching the ON CONFLICT` porque `rate_limits` nunca tuvo UNIQUE sobre (identifier, identifier_type, action_type). Sintoma corroborante: `rate_limits` con 0 filas historicas pese a que content-ai "limita" cada request. Bug 2 arreglado en `20260811190053_rate_limits_unique_constraint_fix.sql` (verificado: 4a llamada con max 3 -> allowed:false). Bug 1 SIGUE ABIERTO a proposito: arreglarlo empezaria a bloquear de golpe trafico de produccion que hoy pasa libre (content-ai, board-ai, bunny-*, etc.). Quien lo arregle debe revisar antes si los presets (ai=20/min) son realistas para el uso actual.
2026-08-11 | `user_notifications.type` es TEXT LIBRE en la BD (sin enum, sin CHECK — la unica constraint de la tabla es el PK). PERO el frontend NO tolera types nuevos: `KiroNotificationBridge.ts` hace `PLATFORM_NOTIFICATION_MAP[type]` sin fallback -> `undefined.zone` -> TypeError. En el fetch por lote de `useKiroPlatformSync.ts` el `.map()` lanza y tumba las 50 notificaciones de esa carga, no solo la fila mala. Ya pasa HOY en produccion con `type='message'` (lo inserta el trigger de chat y no esta en el mapa). Types seguros: content_update / mention / assignment / status_change / recruitment_request. Para discriminar features nuevas usar `entity_type`, NO inventar un `type`. Ojo: `useUserNotifications.ts` es codigo muerto en runtime (nadie lo invoca); el consumidor real es `useKiroPlatformSync.ts`.
2026-08-11 | La politica "Org members can manage clients" de `clients` filtra por `role IN ('admin','team_leader','strategist')` pero en produccion `organization_members` tiene CERO filas con `team_leader` o `strategist`. Los roles reales hoy: creator(188), content_creator(103), client(51), editor(21), creative_strategist(5), digital_strategist(4), admin(4). En `clients` el bug queda tapado porque hay otras 12 politicas PERMISSIVE que dan acceso por otras rutas — pero copiar ese predicado a una tabla con UNA sola politica deja fuera a todos los estrategas. Al escribir RLS nueva incluir ambos sets (legacy + canonicos). Mismo patron de deuda que el bug de `creator` vs `content_creator`.
2026-08-11 | El MCP de Supabase SI puede deployar edge functions con imports `../_shared/` — contradice la nota previa de "siempre CLI". El truco es pasar el array `files` con las rutas relativas al directorio `supabase/functions/`: entrypoint `mi-funcion/index.ts` + entradas `_shared/cors.ts`, `_shared/loquesea.ts`. Verificado deployando client-onboarding-get y client-onboarding-submit (ambas ACTIVE v1, imports resueltos, funcionando en prod). Util cuando `SUPABASE_ACCESS_TOKEN` esta en formato legacy y el CLI no autentica (`LegacyInvalidAccessTokenError`).
2026-08-11 | `client_onboarding_forms` (nueva): formulario que el cliente llena SIN sesion via token de 64 hex. UNA sola politica RLS (org staff), CERO politicas anon — anon recibe `42501 permission denied` a nivel de GRANT, antes de RLS. Acceso publico solo por `client-onboarding-get` / `client-onboarding-submit` (verify_jwt=false + service role). Los datos legales/fiscales (NIT, direccion fiscal, representante) viven SOLO ahi y NO se copian a `clients` porque `clients.is_public` tiene DEFAULT true + politica SELECT para anon. Fase 3 (volcado manual a clients/products, status -> processed) pendiente.
2026-08-11 | Respaldo pre-simplificacion (rama `simplificacion-2026`, tag `pre-simplificacion`): el set de 5 modulos a eliminar (live streaming, social/feed, UP/reputacion, marketplace de campanas, booking) son 135 tablas / 86.024 filas. Dependencias medidas: 8 FKs ENTRANTES desde tablas que se quedan (alerts, brand_credit_transactions, creatives, marketplace_media, marketplace_projects.application_id + .campaign_id, portfolio_items.legacy_post_id, scheduled_posts) -> bloquean el DROP; 2 vistas (una matview `season_leaderboard_live`) y 133 funciones que mencionan tablas del set. La de mayor riesgo: `admin_delete_user_cascade` toca 31 tablas del set — si se dropean sin reescribirla, borrar usuarios revienta (y ese es justo el camino que evita zombies). Detalle en backups/pre-simplificacion/schema/06_dependencias.md.
2026-08-11 | 23 de esas 135 tablas devuelven `42501 permission denied` a service_role via PostgREST: les falta `GRANT ALL ... TO service_role` (mismo patron del incidente academy_*). Lista en manifest.json (campo `via: mcp-postgres`): hashtags, post_hashtags, profile_views, saved_searches, reputation_configs, reputation_global, role_multipliers, role_points_config, role_weight_config, season_goals, season_reward_claims, season_rewards, up_arbiter_log, up_chronometer_pauses, up_client_trust_scores, up_creadores(+_totals), up_editores(+_totals), up_user_scores, campaign_deliverables, campaign_invitations, campaign_media, campaign_notifications, activation_publications, publication_verification_queue, managed_campaign_subscriptions, creator_availability. Cualquier edge function con service_role que las lea da 500 silencioso.
2026-08-11 | Linea base pre-simplificacion medida (docs/SIMPLIFICACION_BASELINE.md): build 2m17s, dist 22,2 MB / 390 chunks JS, 165 `<Route`, 160 paginas, 1.946 archivos .ts(x) / 518.084 lineas en src, 169 edge functions, 217 migraciones, CERO tests automatizados (no hay script test ni vitest/jest ni archivos *.test.*). Los chunks `dash.all.min` (854 kB) + `hls` (525 kB) son los players de live: 1,38 MB que deben desaparecer completos si el borrado de streaming queda bien hecho.
2026-08-11 | Simplificacion 2026 BLOQUE 0 ejecutado (rama simplificacion-2026). Se reescribieron las 5 piezas vivas que dependian de tablas a dropear, ANTES de tocar ninguna tabla: (1) `auto_approve_stale_content()` sin reputation_events/reputation_seasons/sync_marketplace_reputation — el criterio de seleccion del cursor quedo identico caracter por caracter (verificado con substring sobre prosrc); cron jobid 16 sigue vivo. (2) `admin_delete_user_cascade()` de 111 a 91 DELETE: se quitaron 20 (UP, campaign_applications, creator_availability, social_notifications) y se CONSERVARON a proposito portfolio_posts/portfolio_stories/portfolio_post_comments porque esas tablas viven hasta el bloque 5. Verificado por conjuntos Y por orden exacto de sentencias (el orden importa por FKs). Probada end-to-end con RAISE EXCEPTION para forzar rollback: success=true, 12 etiquetas de deleted_from intactas. (3) `kreoon_merge_client()` sin los 10 reapuntes a live/streaming/company_followers/marketplace_campaigns. (4) gate de referidos (`trigger_check_referrer_unlock`, `count_qualified_referrals`, `get_referral_gate_status`) migrado de portfolio_posts a portfolio_items. (5) edge function `evaluate-profile-tokens` desplegada sin la lectura de user_achievements. Ademas se elimino `trg_escrow_create_streaming_session` de escrow_holds (los otros 2 triggers del escrow intactos).
2026-08-11 | BUG PREEXISTENTE encontrado y corregido en el bloque 0: `count_qualified_referrals` y `get_referral_gate_status` referenciaban `portfolio_items.creator_profile_id`, columna que NO EXISTE (la real es `creator_id`). count_qualified_referrals lanzaba 42703 SIEMPRE; el error quedaba oculto porque `trigger_check_referrer_unlock` lo traga con EXCEPTION WHEN OTHERS y get_referral_gate_status solo lo esquivaba en el return temprano de usuario ya desbloqueado. O sea: el gate de referidos llevaba tiempo inoperante. Impacto del arreglo hoy = 0 desbloqueos nuevos porque `referral_relationships` tiene 0 filas. Con la semantica corregida, de los 523 creator_profiles activos calificarian 83 en vez de 22 si algun dia se pueblan las relaciones.
2026-08-11 | `chronometer_pauses` NO es del board: 0 triggers activos, 0 llamadas desde src/ (check_and_pause_chronometer / resume_chronometer / get_content_paused_hours no tienen ningun caller en el frontend) y solo aparece dentro de admin_delete_user_cascade. Es codigo muerto del arbol UP -> se dropea con el bloque 4. El mapa lo tenia como DUDOSO; queda resuelto.
2026-08-11 | El MCP de Supabase devuelve SOLO el resultado del ultimo statement de un execute_sql multi-sentencia. Para inspeccionar el resultado de algo ejecutado dentro de BEGIN/ROLLBACK, envolverlo en un `DO $$ ... RAISE EXCEPTION 'resultado=%', r; END $$` — la excepcion aborta la transaccion (rollback garantizado) y el JSON viaja en el mensaje de error. Verificado ademas que BEGIN/ROLLBACK aisla de verdad en este canal (probe con CREATE TABLE revertido).
2026-08-11 | Simplificacion 2026 BLOQUE 1 (booking) ejecutado y cerrado. 15 tablas + 1 vista (creator_availability_status) + 12 funciones + 2 enums dropeados; 4 archivos borrados (useCreatorAvailability.ts y las 3 edge functions calendar-google-*, retiradas tambien del proyecto Supabase con `supabase functions delete`). El DROP enseño dos ordenes obligatorios: `get_creator_availability(uuid)` DEVUELVE el row-type de la tabla -> hay que dropear la funcion ANTES que la tabla; `auto_booking_event_type_slug()` tiene un trigger encima -> hay que dropearla DESPUES. Orden final que funciona: vista -> RPCs -> tablas -> funciones de trigger -> enums. apply_migration del MCP es ATOMICO: los 2 intentos fallidos no dejaron nada a medias.
2026-08-11 | El mapa de eliminacion daba booking como "0 dependencias inversas" y HABIA una: `supabase/functions/ai-creator-matching/index.ts` consultaba `creator_availability` para puntuar disponibilidad (0-10 puntos del score de matching). No aparecio porque el barrido de dependencias inversas se hizo sobre src/ y no sobre supabase/functions/. Reescrita para leer `creator_profiles.is_available` (true=+10, false=0, null=+7 igual que antes) y redesplegada. REGLA para los bloques 2-5: hacer el grep de nombres de tabla tambien sobre supabase/functions/, y repetirlo DESPUES del DROP.
2026-08-11 | `npx tsc --noEmit -p tsconfig.app.json` en este repo tarda >10 minutos (1.945 archivos / 518k lineas) — no sirve como check rapido en medio de una sesion; lanzarlo en background. `npm run build` (vite+SWC) tarda ~4 min y ya detecta imports rotos.
